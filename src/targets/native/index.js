const createIOSSimulatorTarget = require('./server/ios-simulator');
const createAndroidEmulatorTarget = require('./server/android-emulator');

module.exports = {
  createIOSSimulatorTarget,
  createAndroidEmulatorTarget,
};
